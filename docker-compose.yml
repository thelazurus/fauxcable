services:
  fauxcable:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./generics:/app/generics
      - ./fonts:/app/fonts
      - ./config.yaml:/app/config.yaml
    restart: unless-stopped
